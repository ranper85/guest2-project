apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend
  namespace: grupp1  
spec:
  to:
    kind: Service
    name: frontend
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
    
 
   
